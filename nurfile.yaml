functions:
  - name: nur-test-function
    directory: /
    template: rust
    build:
      command: cargo build --target wasm32-wasip1 --release
      output: target/wasm32-wasip1/release/nur-test.wasm

